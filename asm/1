
指令前缀 

锁定前缀 重复前缀    指令独占内存   important for 多处理器 多线程 ； 重复前缀  表示重复功能

段覆盖前缀 分支提示前缀    覆盖定义的段寄存器值的指令; 条件跳转语句最可能的路径的线索

操作数长度覆盖前缀		程序在这个操作码之内切换16和32的操作数长度    使用大长度的操作数时警告处理器，帮助加快对寄存器的数据赋值

地址长度覆盖前缀 切换不同位数的内存地址  



修饰符  涉及到什么寄存器和内存位置   三个单独的值   寻址方式说明符 比例索引基址   地址移位字节
ModR/M字节
SIB
移位

ModR/M Mod reg/opcode r/m

比例 索引 基址

.session 定义内存段 汇编语言程序在其中定义元素    
汇编语言程序都至少具有三个必须声明的段落 数据段 bss段 文本段


处理器主要组件 控制单元 执行单元 寄存器 标志



A& 使用$表示立即操作数   intel不需界定
寄存器前缀%          intel不这样做
处理源和目标操作数使用相反的顺序   movl $4, %eax
在助记符后使用一个单独的字符引用操作中使用的数据长度， intel语法中数据长度被声明为单独的操作数

as汇编器  ld连接器
